<!DOCTYPE html>
<html>
<head>
	<title>Добавление новых значений</title>
	<%- include("../common/head.ejs")%>
	<link rel="stylesheet" type="text/css" href="/css/pps/add-value.css">
</head>
<body>
	<%- include("../common/menu.ejs") %>
	<div class="wrapper container-fluid">
		<div class="content">
			<h2 class="ml-md-1">Отметить выполнение ПЭД</h2>
			<hr>
			<div class="row">
				<div class="col-sm-3 px-0 px-sm-2 mx-0">
					<aside>
						<ul class="treeCSS">
							<li>Выберите ПЭД
								<ul>
									<% for (key in obj) {%>
									<li><div class="drop dropM">-</div><%=key%>
										<ul>
											<% for(key2 in obj[key])  {%>
												<% if(key2 != 'nosubtype') {%>
												<li><div class="drop dropM">-</div><%=key2%>
													<ul>
												<%} %>
												<% for(let i = 0; i < obj[key][key2].length; i++) {%>
													<li><a class="ref" id = <%=obj[key][key2][i]['name'] + ""%>  href="" title="<%=obj[key][key2][i]['description']%>"><%=obj[key][key2][i]['name']%></a></li>
												<%} %>
												<% if(key2 != 'nosubtype') {%>
													</ul></li>
												<%} %>
											<%} %>
										</ul>
									</li>
									<%} %>
								</ul>
							</li>
						</ul>
					</aside>
				</div>
				<div id="tablekpi" class="tablekpiborder col-sm px-0 mx-0 mr-sm-2 p-3"></div>
			</div>


		<div class="footer">
			<%- include("../common/footer.ejs") %>
		</div>

	</div>
	<%- include("../common/scripts.ejs")%>
	<script type="text/javascript" src="/scripts/pps/add-value.js"></script>
	<script type="text/javascript" src="/scripts/custom-file.js"></script>
</body>
</html>