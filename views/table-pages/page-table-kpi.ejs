<!DOCTYPE html>
<html>
<head>
	<title>Список Показателей</title>
	<%- include("../common/head.ejs")%>
</head>
<body>
	<%- include("../common/menu.ejs") %>
	<div class="wrapper">
		<div class="content container-fluid">
			<h1>Список Показателей эффективности деятельности</h1>
			<% if(kpi) {%>
			<div class="table-responsive-md">
				<table class="table border-color-f table-sm">
					<thead>
						<tr class="row1">
							<th scope="col" rowspan="2">Шифр показателя эффективности</th>
							<th scope="col" rowspan="2">Показатель</th>
							<th scope="col" rowspan="2">Критерии оценки эффективности деятельности</th>
							<th scope="col" colspan="<%=positions.length%>">Группы</th>
						</tr>
						<tr class="row2">
							<% for(let i = 0; i < positions.length; i++) {%>
								<th scope="col"><%=positions[i]%></th>
							<%} %>
						</tr>
					</thead>
					<tbody>
						<% for(let u = 0; u < kpi.length; u++) {%>
							<% if(kpi[u].length != 0) {%>
								<tr>
									<th scope="row"><%=kpi[u][0].section[0] + "." %></th>
									<td colspan="<%=positions.length + 2%>" class="namesection">
										<h4>Раздел <%=(u+1) + ". " + kpi[u][0].section %></h4>
									</td>
								</tr>
								<% let arr = kpi[u]; %>
								<% for(let i = 0; i < arr.length; i++) {%>
									<% if(arr[i].type == 1) {%>
										<tr>
											<th scope="row" rowspan="<%=arr[i].count_criterion%>"><%=arr[i].name %></th>
											<td rowspan="<%=arr[i].count_criterion%>"><%=arr[i].description %></td>
											<% for(let j = 0; j < arr[i].lines.length; j++) {%>
												<% if(j != 0) {%>
													</tr><tr>
												<%} %>
												<td><%=arr[i].lines[j].name %></td>
												<% for(let k = 0; k < positions.length; k++) {%>
													<td><big><%=arr[i].lines[j].balls[positions[k]] %></big></td>
												<%} %>
											<%} %>
										</tr>
										<%} %>
										<% if(arr[i].type == 2) {%>
										<tr>
											<th scope="col" rowspan="<%=(arr[i].count_criterion + 1)%>"><%=arr[i].name %></th>
											<td colspan="<%=positions.length + 2%>"><%=arr[i].description %></td>
											</tr>
											<% for(let j = 0; j < arr[i].lines.length; j++) {%>
											<tr>
												<td><%=arr[i].lines[j].description %></td>
												<td><%=arr[i].lines[j].name %></td>
												<% for(let k = 0; k < positions.length; k++) {%>
													<td><big><%=arr[i].lines[j].balls[positions[k]] %></big></td>
												<%} %>
											<%} %>

										</tr>
										<%} %>
								<%} %>
							<%} %>
						<%} %>
					</tbody>
				</table>
			</div>
			<%} %>
			<% if(!kpi) {%>
			<h4>Нет Показателей эффективности деятельности</h4>
			<%} %>
		</div>
		
		<div class="footer">
			<%- include("../common/footer.ejs") %>
		</div>
	</div>

	<%- include("../common/scripts.ejs")%>
	
</body>
</html>